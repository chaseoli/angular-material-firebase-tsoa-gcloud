import { assert} from 'chai';
import { describe } from 'mocha';

// Test Example
describe('TODO: write unit tests', () => {
    let val = false;
    val = true;
    it('expect result to be true', () => {
        assert.equal(val, true);
    });
});
